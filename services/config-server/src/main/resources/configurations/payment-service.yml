spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payment
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    producer:
      bootstrap-servers: localhost:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.type.mapping: paymentConfirmation:com.salam.ecommerce.notification.PaymentNotificationConfirmation
  server:
    port: 8060
#application:
#  config:
#    customer-url: http://localhost:8090/api/v1/customers
#    payment-url: http://localhost:8060/api/v1/payments
#    product-url: http://localhost:8050/api/v1/products
